# Python cache
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.so
*.egg-info/
pip-wheel-metadata/

# Virtual env
env/
venv/
.venv/
ENV/
env.bak/
venv.bak/

# IDE/Editor
.vscode/
.idea/
*.code-workspace

# OS
.DS_Store
Thumbs.db
desktop.ini

# Logs
logs/
*.log

# Tests/Coverage/Linters
.pytest_cache/
.coverage
.coverage.*
coverage.xml
htmlcov/
.mypy_cache/
.ruff_cache/

# Secrets / Certs
.env
*.env
**/.env
.env.*
dist/.env
dist_*/.env
cert.pem
key.pem

# Build & Distribution
build/
dist/
dist_*/
*.spec
*.bak
*.tmp

# Executables in dist
dist/**/*.exe
dist_*/**/*.exe

# Node (se presente)
node_modules/

# Billing data — plain JSON mai in repo (esiste solo owner_billing.enc cifrato)
owner_billing.json

# File locali vari non versionati
waitress-serve
sviluppo/
per chiudere eventuali scontrini aperti che bloccano RCH

# ============================================================
# DATI: mai nel repository.
# Il 21/08/2026 suncity.db, sunexpress.db,
# clienti_importazione_con_tessera.csv e import_suncity_passo2.sql
# erano finiti su GitHub con dentro nome, cognome e cellulare di
# clienti veri. Sono stati tolti da tutta la storia con git filter-repo.
# Queste righe servono perche' non ci tornino.
# ============================================================
*.db
*.sqlite
*.sqlite3
*.csv
import_*.log
import_*_passo*.sql

# Registro dei trattamenti (art. 30 GDPR): documento INTERNO, si esibisce solo
# all'Autorita' di controllo. Il repository e' pubblico, quindi non ci va.
# Se un domani il repo tornasse privato, questa riga si puo' togliere.
contratto/REGISTRO_TRATTAMENTI.md
